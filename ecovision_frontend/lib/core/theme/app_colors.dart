import 'package:flutter/material.dart';

/// Định nghĩa màu sắc cho toàn app
class AppColors {
  // Gradient background
  static const gradientStart = Color(0xFF00D9A3);
  static const gradientEnd = Color(0xFF0099FF);
  
  // Primary colors
  static const primary = Color(0xFF00CC88);
  static const primaryLight = Color(0xFF00D9A3);
  
  // Status colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
  static const info = Color(0xFF2196F3);
  
  // Text colors
  static const textWhite = Colors.white;
  static const textWhite70 = Colors.white70;
  
  // Card colors
  static const cardOverlay = Colors.white24;
  static const cardWhite = Colors.white;
}
